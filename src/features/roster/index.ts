/**
 * Roster feature.
 *
 * Filled in Phase 3: `RosterScreen`, the `useRoster` hook returning `{ state, onEvent }`,
 * and the `RosterUiState` discriminated union — including the Loading, Empty and Error
 * states the prototype does not have (ARCHITECTURE.md §8).
 *
 * This module may not import core/db or core/network, and may not import features/player.
 */

export {};
