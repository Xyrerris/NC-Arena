/**
 * Player detail feature.
 *
 * Filled in Phase 4: `PlayerDetailScreen` with the Stats / Vs You segmented tabs,
 * `usePlayerDetail`, and the comparison-bar rows.
 *
 * This module may not import core/db or core/network, and may not import features/roster.
 */

export {};
