/**
 * Test doubles and helpers. Allowed to import everything, because it fakes everything.
 *
 * Filled in Phase 2: the in-memory database factory (better-sqlite3, so query tests run
 * in Node with no emulator), repository fakes, and render helpers.
 */

export {};
