/**
 * Design system — tokens and shared components.
 *
 * Filled in Phase 1: colour/spacing/radii tokens, text-alpha tokens clamped at α ≥ 0.50
 * (ARCHITECTURE.md §2.4), the `ArenaText` primitive, and StatRow / CompareBar / SortChip /
 * RecordBadge / ViewerCard / SearchField / SegmentedTabs / ScreenScaffold.
 *
 * This is also the only module allowed to contain `Platform.select`, so that keeping the
 * app iOS-capable (ARCHITECTURE.md §9.6) never leaks platform forks into feature code.
 */

export {};
